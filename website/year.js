        // Get the current year
        const currentYear = new Date().getFullYear();

        // Insert the current year into the element with the id "currentYear"
        document.getElementById("currentYear").textContent = currentYear;
