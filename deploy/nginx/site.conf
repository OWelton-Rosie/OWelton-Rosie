server {
    listen 80;
    server_name _;

    root /var/www/oweltonrosie;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /_app/immutable/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}